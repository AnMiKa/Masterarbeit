function [ f ] = upper_obj( X, Y, W )
%UNTITLED11 Summary of this function goes here
%   Detailed explanation goes here

[~,J,T] = size(x);
f = 0;
for t =  1:T
    Xval = X(:,:,t);
    Yval = Y(:,t);
    f = f + 1/(2*J)*sum(sum((Xval'*W-Yval).^2,1));
end
f = 1/t*f;
end

